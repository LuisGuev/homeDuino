- To compile, Load sketch onto Arduino with ethernet shield
- Load index.htm into the SD card (formatted as FAT32)
- Plug in arduino to power source along with plugging in an ethernet cable
- May have to change IP, depending on the Arduino's IP 
- Use any browser, type in Arduino's IP
- Webpage will be loaded, now you can control the arduino from a device
that is connectected to the same network